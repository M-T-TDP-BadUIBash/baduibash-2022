Prototypes are shared styles that create the basics of a component or element.
On their own prototype should not be used, they should be used by including the mixin
with in a class and extending it to be unique.

If you are editing a prototype you should know exactly what it is going to effect,
small changes here can have large scale consequences.